defmodule Solution931 do
  @spec min_falling_path_sum(matrix :: [[integer]]) :: integer
  def min_falling_path_sum(_matrix) do
    # size = Enum.count(matrix)
    # helper(0, 0, 0, matrix)
    0
  end

  # defp val(matrix, x, y), do: matrix |> Enum.at(x) |> Enum.at(y)
end
